name: running_sonar.yml
on:
#  schedule:
#    - "0 0 * * *"
  push:
    branches:
      - main

jobs:
  build-sonar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup JAVA
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin
      - name: Setup MAVEN
        uses: stCarolas/setup-maven@v5
        with:
          maven-version: '3.8.2'
      - name: RUN LS
        run: |
          ls -la
      - name: Realiza Analise do Sonar
        run: |
          mvn clean verify -DskipTest sonar:sonar -Dsonar.projectKey=Cervejaria-ACME -Dsonar.projectName='Cervejaria ACME' -Dsonar.host.url=http://ec2-35-90-57-172.us-west-2.compute.amazonaws.com:9000 -Dsonar.token=sqp_c58e30e4cae1a5c3225cd4e60f784cb88b9220c5
          
          
